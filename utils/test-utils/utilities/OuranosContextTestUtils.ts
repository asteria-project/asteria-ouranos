/*!
 * This module constains utilities used by the OuranosContext test suite.
 */

import * as uuid from 'uuid';

// Utilities:
// We store constants that should be defined by the OuranosContext class:
export const CLASS_NAME: string = 'com.asteria.ouranos.core::OuranosContext';
export const NAME: string = 'contextName';
export const UUID: string = uuid.v4();