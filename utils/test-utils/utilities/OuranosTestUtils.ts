/*!
 * This module constains utilities used by the Ouranos test suite.
 */

import { AsteriaSessionConfig } from 'asteria-gaia';

// Utilities:
// We store constants that should be defined by the Ouranos class:

export const CLASS_NAME: string = 'com.asteria.ouranos.core::Ouranos';
export const STREAM_PROCESS_CONFIG: any = { test: 'test config' };
export const SESSION_CONFIG: AsteriaSessionConfig = { name: 'test-config' };